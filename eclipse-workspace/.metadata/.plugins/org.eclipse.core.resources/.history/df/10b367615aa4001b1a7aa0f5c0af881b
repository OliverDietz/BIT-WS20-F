import java.util.ArrayList;

public class Test {
 static ArrayList<MP3> TitleList=new ArrayList<MP3> ();
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		MP3 Titel1;
		Titel1=new MP3();
		Titel1.set_titel("In the End");
		TitleList.add(Titel1);
		
		MP3 Titel2;
		Titel2=new MP3();
		Titel2.set_titel("Castle of glass");
		TitleList.add(Titel2);
		
		String testReturnTitle=Titel1.get_titel();
		for(MP3 currentTitle:TitleList) {
			System.out.println(currentTitle.get_titel());
		}
		for(MP3 currentTitle:TitleList) {
		System.out.println("Adress of current title is:" + currentTitle +" - "+currentTitle.get_titel());
		}
	}

}
